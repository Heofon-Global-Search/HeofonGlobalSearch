import React from "react";
import ReactDOM from "react-dom/client";

ReactDOM.createRoot(
  document.getElementById("root")!
).render(
  <h1 style={{
    color: "red",
    fontSize: "60px"
  }}>
    TEST FROM MAIN TSX
  </h1>
);
